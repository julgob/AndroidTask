<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="utt.if26.androidtask">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_checked_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_checked_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CreatePassWordActivity"></activity>
        <activity android:name=".AuthenticateActivity" ><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".EntityActivity" />
        <activity android:name=".ui.EditReminderActivity" />

        <receiver
            android:name=".receiver.ReminderReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".NavigationDrawerActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>